import styled from 'styled-components';
import Animation from './Animation';

const Button = styled(Animation)`
  width: 80px;
  height: 30px;
  border-radius: 4px;
  background-color: gray;
`;

export default Button;
