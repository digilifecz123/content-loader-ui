import styled from 'styled-components';
import Animation from './Animation';

const ImagePlaceholder = styled(Animation)`
  background: #efefef;
  height: 150px;
  width: 200px;
  border-radius: 5px;
`;

export default ImagePlaceholder;
